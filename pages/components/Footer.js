export default function Header({ stands }) {
  return (
    <footer className=" p-4 bg-green-300 text-black">
      <h1 className="ml-0">{stands.length} Locations World Wide</h1>
    </footer>
  );
}
