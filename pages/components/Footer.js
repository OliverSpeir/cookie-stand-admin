export default function Header({ locations }) {
    return (
        <footer className=' p-4 bg-green-300 text-black'>
        <h1 className="ml-0">{locations} Locations World Wide</h1>
      </footer>
    );
}